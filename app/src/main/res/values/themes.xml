<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AndroidToolbox" parent="android:Theme.Material.Light.NoActionBar" />
    <!-- Material 3 Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/md3_splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidToolbox</item>
    </style>
</resources>